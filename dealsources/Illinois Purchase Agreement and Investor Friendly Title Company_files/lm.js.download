function get(parameter,URL){const reg=new RegExp(`[?&]${parameter}=([^&#]*)`,"i");const string=reg.exec(URL);return string?string[1]:undefined}function checkUrl(param){const url=window.location.href;if(get(param,url)){return get(param,url)}return false}function createCookie(name,value,hours,origin,test){let expires="";if(hours){const date=new Date;date.setTime(date.getTime()+hours*60*60*1e3);expires=`; expires=${date.toUTCString()}`}if(test){document.cookie=`${name}=${value}${expires};path=/`}else{document.cookie=`${name}=${value}${expires};domain=${origin}; path=/`}}var getCookies=function(){var pairs=document.cookie.split("; ");var cookies={};for(var i=0;i<pairs.length;i++){var pair=pairs[i].split("=");cookies[pair[0]]=unescape(pair[1])}return cookies};function lmFinished(){const cookies=getCookies();return cookies.lmref}(()=>{const location=window.location.host;const host=location.split(".");const chunky=host.slice(-2);const origin="."+chunky.join(".");const r=checkUrl("lmref");const t=checkUrl("lmref-test");if(r){createCookie("lmref",r,2160,origin,false)}if(t){createCookie("lmref",t,1,origin,true)}})();